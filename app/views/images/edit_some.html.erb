<%= form_tag admin_images_path do %>
	<% @gor_clothing.images.each do |image| %>
	<span class = "collateral_images_path"%>
		<span id = "images_<%= image.type_of_image %>">
			<%= image_tag image.picture.url if image.picture? %>
		</span>
	</span>
	<%end%>
<%end%>